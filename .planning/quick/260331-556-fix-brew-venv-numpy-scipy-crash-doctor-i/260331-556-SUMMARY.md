---
quick_task: 260331-556
date: 2026-03-31
status: complete
---

# Quick Task 260331-556 Summary

Fixed the Homebrew venv crash where `scipy.special` import fails with:
`ValueError: All ufuncs must have type numpy.ufunc ... sph_legendre_p`.

## Changes

- Pinned MLX venv deps to a safer combo in `requirements-mlx.txt`:
  - `numpy==2.2.6`
  - `scipy==1.16.2`
- Hardened `chatter doctor` to run a small Python import sanity check for `numpy` and `scipy.special` and fail fast with a clear remediation hint if imports fail.
- Fixed `src/commands/validate.rs` unit test to avoid matching valid tags from the “valid tags” list line.

## Verification

- `cargo test` passes locally.

