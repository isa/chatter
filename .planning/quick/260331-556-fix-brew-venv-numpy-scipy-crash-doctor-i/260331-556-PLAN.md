---
quick_task: 260331-556
date: 2026-03-31
description: Fix brew venv numpy/scipy crash + doctor import check
---

# Quick Task 260331-556: Fix brew venv numpy/scipy crash + doctor import check

## Objective

Prevent Homebrew installs from shipping a broken scientific stack (NumPy/SciPy) that crashes at runtime (e.g. `scipy.special` import), and make `chatter doctor` catch this failure before users hit `clone`/`generate`.

## Tasks

1. Update MLX requirements pins to a compatible NumPy/SciPy pair.
2. Add a lightweight Python import sanity check (`numpy`, `scipy.special`) to `doctor`.
3. Ensure tests still pass.

## Files

- `requirements-mlx.txt`
- `src/bridge/doctor.rs`
- `src/commands/doctor.rs`
- `src/commands/validate.rs` (test fix)

