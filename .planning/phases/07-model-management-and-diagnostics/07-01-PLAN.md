---
phase: 07-model-management-and-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bridge/model.rs
  - src/commands/model.rs
autonomous: true
requirements: [MDL-01, MDL-03]

must_haves:
  truths:
    - "chatter model list groups models by engine (Qwen section, ChatterBox section)"
    - "chatter model list shows variant name (Original, Turbo, Multilingual) alongside repo ID for ChatterBox"
    - "chatter model download --engine chatterbox shows estimated total size and available disk space before starting"
    - "chatter model download --engine chatterbox warns if less than 5GB would remain after download"
  artifacts:
    - path: "src/bridge/model.rs"
      provides: "disk_space_check() function, variant_label() helper, engine field on ModelInfo"
      contains: "disk_free_bytes"
    - path: "src/commands/model.rs"
      provides: "Engine-grouped list output, disk space pre-check before ChatterBox download"
      contains: "ChatterBox Models"
  key_links:
    - from: "src/commands/model.rs"
      to: "src/bridge/model.rs"
      via: "disk_space_check() call before download_model_chatterbox()"
      pattern: "disk_space_check"
---

<objective>
Enhance `chatter model` commands with engine-grouped listing and disk space awareness for ChatterBox downloads.

Purpose: Users need to see which models belong to which engine and understand disk space requirements before downloading large ChatterBox models (~15-20GB total).
Output: Enhanced model list with engine sections and variant labels; disk space pre-check on ChatterBox download.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/bridge/model.rs:
```rust
pub struct ModelInfo {
    pub repo_id: String,
    pub size_label: String,
    pub local_path: Option<String>,
    pub size_bytes: Option<u64>,
}

pub fn chatterbox_model_variants() -> Vec<String> {
    vec![
        "ResembleAI/chatterbox".to_string(),
        "mlx-community/chatterbox-fp16".to_string(),
        "mlx-community/chatterbox-turbo-fp16".to_string(),
    ]
}

pub fn download_model_chatterbox() -> Result<(), BridgeError>;
pub fn list_cached_models() -> Result<Vec<ModelInfo>, BridgeError>;
```

From src/commands/model.rs:
```rust
pub fn run(command: ModelCommands, global: &GlobalArgs) -> anyhow::Result<()>;
fn format_bytes(bytes: u64) -> String;
```

From src/cli.rs:
```rust
pub enum Engine { Qwen, Chatterbox }
pub enum ModelCommands { Download { variant: ModelVariant }, List, Remove }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disk space check and engine/variant fields to bridge model layer</name>
  <files>src/bridge/model.rs</files>
  <read_first>
    - src/bridge/model.rs
    - src/bridge/doctor.rs (for disk space detection pattern using shutil)
    - src/bridge/error.rs
  </read_first>
  <action>
Modify `src/bridge/model.rs` to add:

1. **Add `engine` field to `ModelInfo`** — Add `pub engine: String` field to the `ModelInfo` struct. Set it to `"qwen"` for Qwen models and `"chatterbox"` for ChatterBox models in `list_cached_models()`. This is the data layer for D-05 engine-grouped listing.

2. **Add `variant_label` field to `ModelInfo`** — Add `pub variant_label: Option<String>` field. For ChatterBox models, derive the label from the repo_id:
   - `"ResembleAI/chatterbox"` -> `Some("Original")`
   - repo_id containing `"chatterbox-turbo"` -> `Some("Turbo")`
   - repo_id containing `"chatterbox"` and containing `"multilingual"` -> `Some("Multilingual")`
   - For any other chatterbox repo_id -> `Some("ChatterBox")`
   - For Qwen models -> `None`

3. **Add `disk_space_check()` function** that returns `(u64, u64)` — `(free_bytes, estimated_download_bytes)`. Per D-02, this shows estimated total size and available disk space. Implementation:
   - Use Python via `pyo3::Python::attach` to call `shutil.disk_usage("/")` to get free bytes (same pattern as `bridge/doctor.rs::get_disk_free_gb`).
   - Estimated ChatterBox total size: hardcode `20_000_000_000u64` (approximately 20GB for all 3 variants per research). Return this as the second element.
   - Signature: `pub fn disk_space_check() -> Result<(u64, u64), BridgeError>`
   - If shutil fails, return `BridgeError::Other("Failed to check disk space: ...".to_string())`

4. **Update `list_cached_models()`** — Set the new `engine` and `variant_label` fields on each `ModelInfo` pushed to the vec.

Do NOT change `download_model_chatterbox()` itself — the command layer will call `disk_space_check()` before calling it.
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/rust/chatter && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ModelInfo` struct has `pub engine: String` field
    - `ModelInfo` struct has `pub variant_label: Option<String>` field
    - `pub fn disk_space_check()` exists and returns `Result<(u64, u64), BridgeError>`
    - `list_cached_models()` sets `engine` to `"qwen"` or `"chatterbox"` per model
    - `list_cached_models()` sets `variant_label` to `Some("Original")` / `Some("Turbo")` / `Some("Multilingual")` for ChatterBox models
    - `cargo check` succeeds
  </acceptance_criteria>
  <done>ModelInfo has engine and variant_label fields, disk_space_check function exists, list_cached_models populates all fields correctly, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Engine-grouped model list and disk space pre-check in command layer</name>
  <files>src/commands/model.rs</files>
  <read_first>
    - src/commands/model.rs
    - src/bridge/model.rs (after Task 1 changes)
    - src/cli.rs (Engine enum, ModelCommands)
  </read_first>
  <action>
Modify `src/commands/model.rs`:

1. **Update `ModelCommands::List` handler** to group by engine per D-05:
   - After getting `models` from `bridge::model::list_cached_models()`, partition into two vecs: `qwen_models` (where `model.engine == "qwen"`) and `cb_models` (where `model.engine == "chatterbox"`).
   - Print a section header "Qwen3-TTS Models" (bold) followed by a separator line, then list each Qwen model with columns: `repo_id`, `size`, `path`.
   - Print a blank line then section header "ChatterBox Models" (bold) followed by separator line, then list each ChatterBox model with columns: `variant_label` (or repo_id if None), `repo_id`, `size`, `path`.
   - If a section is empty, print "(none downloaded)" under its header.
   - Column format for Qwen: `{:<50} {:>10} {}` (repo_id, size, path) — same as current.
   - Column format for ChatterBox: `{:<14} {:<40} {:>10} {}` (variant, repo_id, size, path).

2. **Add disk space pre-check to `ModelCommands::Download` ChatterBox branch** per D-02 and MDL-03:
   - Before the existing `install_chatterbox_deps()` call, call `bridge::model::disk_space_check()`.
   - Print: `"Estimated download size: {format_bytes(estimated)}"` and `"Available disk space:    {format_bytes(free)}"`.
   - If `free < estimated + 5_000_000_000` (less than 5GB would remain after download per D-02), print a warning: `"Warning: Less than 5 GB will remain after download."` using yellow coloring via `owo_colors` (same pattern as `ui::doctor_warn`). Use `"Warning:".if_supports_color(Stderr, |t| t.yellow())`.
   - Per D-02, auto-proceed (no interactive confirmation) — just inform the user and continue.
   - Print a blank line after the disk space info before proceeding to the download.
   - If `disk_space_check()` fails, print a warning `"Could not check disk space, proceeding anyway..."` and continue (non-blocking).
  </action>
  <verify>
    <automated>cd /Users/isa.goksu/Projects/playgrounds/rust/chatter && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Model list output contains `"Qwen3-TTS Models"` section header
    - Model list output contains `"ChatterBox Models"` section header
    - Empty sections show `"(none downloaded)"`
    - ChatterBox download path calls `disk_space_check()` before `install_chatterbox_deps()`
    - Disk space info printed with `"Estimated download size:"` and `"Available disk space:"`
    - Warning printed when less than 5GB would remain, containing `"Warning:"` in yellow
    - `cargo check` succeeds
  </acceptance_criteria>
  <done>Model list shows engine-grouped sections with variant labels. ChatterBox download shows disk space info and warns if low. cargo check passes.</done>
</task>

</tasks>

<verification>
- `cargo check` passes with no errors
- `cargo clippy` produces no errors (warnings acceptable)
- `list_cached_models()` returns `ModelInfo` with `engine` and `variant_label` fields populated
- Model list command would show grouped output (verified via code review since no models may be cached in CI)
- ChatterBox download path includes disk space pre-check
</verification>

<success_criteria>
- MDL-01: `chatter model list` groups models by engine (Qwen section, ChatterBox section) with variant labels per D-05
- MDL-03: `chatter model download --engine chatterbox` shows estimated size and available space before download, warns if less than 5GB would remain per D-02
- All existing model commands continue to work (download, list, remove for both engines)
</success_criteria>

<output>
After completion, create `.planning/phases/07-model-management-and-diagnostics/07-01-SUMMARY.md`
</output>
